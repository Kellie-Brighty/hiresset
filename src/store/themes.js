import { createTheme } from "@material-ui/core/styles";

export const themes = createTheme({
  palette: {
    primary: {
      main: "#e3682b",
      white: "#fff"
    },
    secondary: {
      main: "#808080",
    },
  },
});
